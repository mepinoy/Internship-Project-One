import { MessageHighlightDirective } from './message-highlight.directive';

describe('MessageHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new MessageHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
